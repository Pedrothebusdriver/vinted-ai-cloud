name: Sampler (Openverse) — Commit Manifest
on:
  workflow_dispatch: {}

jobs:
  sample:
    runs-on: [self-hosted, pi]
    steps:
      - name: Checkout (no built-in creds)
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.name  "${{ secrets.GH_AGENT_NAME || 'pi-agent' }}"
          git config user.email "${{ secrets.GH_AGENT_EMAIL || 'pi@local' }}"

      # Quick sanity: write a tiny manifest so we see a commit right away
      - name: Write tiny manifest
        run: |
          mkdir -p .agent/sampler
          echo '{"ok":true,"ts":"'$(date -u +%FT%TZ)'"}' > .agent/sampler/manifest-test.json
          ls -l .agent/sampler

      - name: Commit
        run: |
          git add .agent/sampler/*.json
          if git diff --cached --quiet; then
            echo "Nothing to commit"; exit 1
          fi
          git commit -m "agent: sampler manifest test $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git log -1 --name-only

      - name: Push using PAT
        env:
          GH_PAT: ${{ secrets.GH_AGENT_PAT }}
        run: |
          git push "https://x-access-token:${GH_PAT}@github.com/${{ github.repository }}.git" HEAD:main
          echo "PUSHED OK"
